# Handover — Outreach Automation (2026-07-02)

Comprehensive state for the next session. Two days of heavy build turned "one manual
campaign" into an automated multi-channel lead engine. Everything is committed + on the
autosave net.

---

## TL;DR
- **Automated & running:** a daily funded-lead engine (250/day, best-first, ~5 months of supply), a LinkedIn reaction engine (proven live), and a 30-min code+data autosave net.
- **Built & ready, needs YOU to load/trigger:** email campaigns for Mailead (816 KSD + 229 funded), the whole funded monthly pipeline, the engagement hunter.
- **The last mile:** wire the Google Sheet → TaskMagic → Mailead auto-send (blocked only on you creating + sharing one sheet).

---

## 1. LIVE & AUTOMATED (running with no intervention)
| System | What it does | Where |
|--------|-------------|-------|
| **Daily funded batch** | 06:30 daily: pull top 250 from the 36,764 scored reserve → truth-check + AI-score → Reoon verify → assemble → `~/Downloads/MAILEAD-Funded-Daily-<date>.csv` → WhatsApp you | launchd `com.kobestarr.funded-daily-batch`; `daily-funded-batch.sh` |
| **Autosave net** | every 30 min + login: snapshots ALL repos under the dev root to `refs/wip/autosave` — now **including `exports/*.csv` data files** | launchd `com.kobestarr.dev-autosave`; `~/.config/dev-autosave/autosave.sh` |
| **Reply-watcher** | (from earlier) on clawdbot, 15-min cron: Gmail → GHL + WhatsApp | clawdbot |

**Recover a clobbered/lost file:** `cd <repo> && git stash apply $(git rev-parse refs/wip/autosave)` or `git checkout refs/wip/autosave -- <path>`.
**Nightly hygiene job** (`com.kobestarr.dev-nightly`) is built but NOT loaded (you never armed it). Arm with `launchctl load ~/Library/LaunchAgents/com.kobestarr.dev-nightly.plist` if wanted.

---

## 2. READY TO LOAD (manual — Mailead has no API)
- **KSD pro-services 816 safe** → `exports/ksd-proservices-batch2-2026-06-24-safe816.csv`. Campaign `KSD · AI Search · Professional Services`, 9 KSD boxes, sequence `exports/pilot-ksd-pro-services-sequence.md`, subject `AI search for {{company_name}} {{first_name}}`.
- **Funded founders 229 safe** → `~/Downloads/MAILEAD-Funded-Founders-229-2026-06-30.csv`. Campaign `KSD · AI Search · LeadByte Funded Founders`, DealFlow boxes, sequence `exports/leadbyte-funded-sequence.md`. Map `company_name` → Company Name.
- Every Mailead load: **NEW dated campaign, never re-upload** (Mailead deletes on re-upload).

---

## 3. IMMEDIATE NEXT ACTIONS (priority order)
1. **Create + share the `ToSend` Google Sheet** → then I wire `push-to-sheet.js` into the daily batch + build the TaskMagic flow (the automation's last mile). Share Editor with `outreach-sheets@kobestarr-leadpipe.iam.gserviceaccount.com`, tab named `ToSend`, send the link.
2. **Load the 816 KSD + 229 funded** into Mailead.
3. **⚠️ Investigate the July 273 cohort:** `exports/funded-startups-2026-07-checked.csv` came out only ~122 bytes (near-empty) after processing the 273 genuinely-new July companies. Either almost none were HOT or something failed — verify before relying on it. Raw new-273 preserved at `exports/funded-drop-NEW-2026-07-01.csv`.
4. **Send the Kondo drafts** (saved, awaiting your send): Jerry Pett, Mark Nickerson (near-closed — chose you, sponsors pending, end-of-month green light), Erin Green (warm), Dennis Postema (guest invite for you), Bill Simone (book feedback).
5. **Build the on-reply Searchable audit generator** (funded emails promise a prepared audit; produce via `api.searchable.com/v1` + MCP on reply).
6. **Prosp connects:** need a target campaign_id + list_id (create in Prosp UI). NOTE: funded LinkedIn handles are 94% broken — drive connects from the **engagement hunter**, not the funded list.

---

## 4. THE TOOLS BUILT (all in outreach-automation unless noted)
| File | Purpose |
|------|---------|
| `score-funded-reserve.js` | rank funded reserve (geo incl. Norway/UAE/rich, recency, size, web-need, ICP role, completeness → /100) |
| `daily-funded-batch.js` / `.sh` | pull top N/day best-first, walk reserve, never repeat; full chain → Downloads + WhatsApp |
| `launch-campaign.js` | one-command "here's the CSV, go" (verify + dedup + naturalise → Downloads + config card) |
| `assemble-funded-mailead.js` | verified cut → Mailead route files A/B/C (naturalised) |
| `process-funded-startups.js` | raw drop → ICP filter + site truth-check + AI-score (`--tag` for distinct cohort names) |
| `verify-funded-startups.js` | Reoon → warm/other/drop (`--tag` override) |
| `verify-csv-reoon.js` | Reoon-verify any CSV (safe/clean split) |
| `linkedin-react-runner.js` | fire `st.reactToPost` via linkedapi.io (type:"like" lowercase), paced/capped/dedup |
| `engagement-hunter.js` | mine commenters on ICP posts → real active ICP-scored warm targets |
| `push-to-sheet.js` | append Mailead-ready CSV to the ToSend staging tab |
| `linkedin-content-intel/src/load-cold-leads.js` | CSV → the Prosp runner's comment_leads DB |
| `docs/funded-monthly-pipeline-sop.md` | the monthly funded runbook |

**Reserve file:** `exports/funded-reserve-ranked.csv` (36,764 scored). Ledger of pulled: `data/funded-reserve-pulled.txt`.

---

## 5. CREDENTIALS & EXTERNAL TOOLS (`~/.credentials/api-keys.json`)
- **linkedapi.io** (`linkedapi_io`): apiKey + identificationToken stored → **reactions LIVE**. Fire: POST `api.linkedapi.io/workflows` `{actionType:"st.reactToPost",postUrl,type:"like"}` (headers `linked-api-token` + `identification-token`).
- **linkdapi** (read/sourcing): header `X-linkdapi-apikey`, base `linkdapi.com/api/v1`. **MUST use curl + browser UA** (Cloudflare 1010 blocks urllib). Endpoints: `/profile/username-to-urn`, `/posts/all?urn=`, `/posts/comments?urn=<activity_urn>`.
- **prosp**: apiKey consolidated into api-keys.json (was only in kobestarr-tools/.env). API works (`getCampaigns`). Needs campaign_id/list_id to fire connects.
- **reoon**: 2,100/day verify.
- **searchable**: Agency plan, `api.searchable.com/v1` + `mcp.searchable.com/mcp` — the on-reply audit engine (per-project).
- **Kondo** (MCP): LinkedIn inbound triage. Needs `app.trykondo.com` open in browser with extension. `read_chat`=local cache, `load_chat`=full history.
- **TaskMagic** (MCP): NOT loaded this session (needs restart to drive via MCP). New version = better campaign sending. Flow (Sheet→Mailead) still to build — in its plain-English builder.
- **Google Sheets SA**: `outreach-sheets@kobestarr-leadpipe.iam.gserviceaccount.com`.
- **WhatsApp**: clawdbot bridge → `447989746146`.

---

## 6. KEY GOTCHAS (hard-won — don't re-learn these)
- **linkedapi.io reaction:** field is `type` (lowercase value `like`), NOT `reactionType`/`LIKE`.
- **linkdapi:** curl + browser User-Agent or you get Cloudflare 1010.
- **Funded LinkedIn URLs are ~94% enrichment guesses** that don't resolve. Email is that list's channel; LinkedIn comes from the engagement hunter (always-real handles).
- **Mailead overwrites on re-upload** → one campaign per batch.
- **Funded email lists are ~46% invalid** → always Reoon-verify first.
- **"New" monthly drops are often re-exports** — dedup by Data Month + against existing (the July-1 pull was 73% duplicate; only 273 new).
- **`linkedin-content-intel` is its own nested git repo** inside kobestarr-tools.
- **Kobi's LinkedIn handle is `kobiomenaka`** (URN `ACoAAALvhooBerHmYU60u1iXgu0OUpCpTXojhc0`), not `kobestarr` (brand).

---

## 7. LEAD INVENTORY
- **Funded reserve: 36,764 scored** (the big engine, ~5 months at 250/day). From a 39,403-row "Prev 1 month" LeadByte export, deduped to genuinely-uncontacted.
- **KSD pro-services: 816 safe** (accountants/solicitors).
- **Funded founders: 229 safe** (routes 187 + LeadByte 43).
- **6,675 KSD B2B** (older Consulti sourcing, still un-emailed).
- DB: `ksd/local-outreach/orchestrator/data/businesses.db`.
- Brand routing: DealFlow = B2B, Stripped = consumer/pop-culture (but ICP audit says Stripped's real revenue is publishers/audio-networks — reconcile). Full ICP system: `kobestarr-tools/.worktrees/clean-icp/strategy/icp/`.

---

## 8. THE STACK IN ONE PICTURE
```
LEADS: monthly LeadByte drop / 36k reserve / KSD DB
  -> score-funded-reserve + daily-funded-batch (auto, 250/day)
  -> process -> Reoon verify -> assemble -> Mailead-ready CSV
EMAIL:  Mailead (manual load today; TaskMagic sheet-flow = the last-mile automation)
LINKEDIN: engagement-hunter (find active ICP) -> react-runner (warm) -> Prosp (connect) -> Kondo (inbound)
REPLIES: reply-watcher -> GHL + WhatsApp ; funded replies -> Searchable audit (to build)
SAFETY: autosave net (30 min, code + data)
```
